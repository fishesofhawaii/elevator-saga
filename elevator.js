{
    init: function(elevators, floors) {
        /*
            NEEDS: 
                On INIT -- elevators should spread out
                On floor button pressed -- evaluate all queues, and pick most logical
                    Direction
                    Load
                    Capacity
                On direction switch -- need to update lights
        */
    },
    update: function(dt, elevators, floors) {

    }
}
